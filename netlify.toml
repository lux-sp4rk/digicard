[build]
  command = "npm run build"
  publish = "dist"

[dev]
  command = "npm run dev"
  framework = "#auto"
  targetPort = 4321

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"
